volumes:
  db-data:
  chrisolsen-goweb-mod_cache:
  caddy_data:
  caddy_config:

networks:
  caddy_net:
  app_net:

services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile.prod
    volumes:
      - .:/app
      - chrisolsen-goweb-mod_cache:/go/pkg/mod

    networks:
      - caddy_net
      - app_net

  caddy:
    image: caddy:latest
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    ports:
      - "80:3000"
      - "443:3000"
      - "443:3000/udp"
    volumes:
      - $PWD/conf:/etc/caddy
      - $PWD/site:/srv
      - caddy_data:/data
      - caddy_config:/config
      - $PWD/.config/Caddyfile.prod:/etc/caddy/Caddyfile

  db:
    image: postgres:latest
    restart: unless-stopped
    volumes:
      - db-data:/var/lib/postgresql/data
